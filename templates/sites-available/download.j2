server {
  listen                        80;
  server_name                   download download.{{ domain }};

  location / {
    root                        /storage/bittorrent/content/;
    autoindex                   on;
  }

  location =/favicon.ico {
    alias                       /usr/share/nginx/html/favicon.ico;
  }
}
